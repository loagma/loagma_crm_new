# Knowlarity Calling — Simple Notes

## Where we are

The "Call" button is already built. It should ring the telecaller, then ring the customer, join both, and save the result. But it **does not work yet** — 2 things are missing.

## Missing

- Username — missing
- API Key — missing
- App Access Key — we have it
- Knowlarity phone number — we have it

**Need to do:** get Username and API Key from Knowlarity, add them to the server. Nothing will work without these.

## Also ask Knowlarity

- The correct web address for our account (one is set now, but may be wrong)
- Confirm we're only doing one call at a time, not bulk calling

## One more step (do this later, when live)

Tell Knowlarity where to send call results. This is set up inside their account, using our real website address (not a test one).

## Test after connecting

- Make one real call, check it saves properly
- Check phone numbers match on both sides (with or without +91)

## Not built yet (only if needed later)

- Pop-up when a call comes in
- Auto-send incoming calls to a specific person
- Backup check for missed results

## Need list

1. Username
2. API Key
3. Correct web address (confirm with Knowlarity)
4. Set up "call result" address in Knowlarity account (once live)
